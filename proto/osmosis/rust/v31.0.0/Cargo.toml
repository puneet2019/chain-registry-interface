[package]
name = "osmosis-proto"
version = "0.1.0-alpha.1"
edition = "2021"
description = "Protobuf Rust types for Osmosis (v31.0.0)"
license = "MIT"

[dependencies]
prost = "0.13"

[dependencies.tonic]
version = "0.12"
optional = true

[features]
default = []
grpc = ["tonic"]
