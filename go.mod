module github.com/puneet2019/chain-registry-interface

go 1.21

// This is the root module file for the chain-registry-interface repository.
// Individual SDK modules are located in subdirectories:
// - go/chainregistry: Go SDK for chain registry interface